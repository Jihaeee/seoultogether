# 위례 아이편한 지도 — P0 백로그

> 작성 2026-08-23 · 대상 릴리스 v0.2 (첫 공개 배포)
> P0 정의: **없으면 서비스가 핵심 사용자 과업을 해결하지 못하는 것.**
> 추정치는 1인 기준 러프 산정이며 스프린트 플래닝에서 재산정 필요.

## 요약

| ID | 제목 | 유형 | 추정 | 상태 |
| --- | --- | --- | --- | --- |
| [WM-1](#wm-1) | 내 위치 상시 표시 · 위치 권한 단일 진입점 | 기능 | 3d | ✅ 구현됨 |
| [WM-2](#wm-2) | 목록 거리순 정렬 (기본 정렬 전환) | 기능 | 2d | ✅ 구현됨 |
| [WM-3](#wm-3) | 마커 클러스터링 — 겹친 21곳 선택 가능하게 | 결함 | 3d | ✅ 구현됨 |
| [WM-4](#wm-4) | 동일 시설명 카드 구분 | 결함 | 0.5d | ✅ 구현됨 |
| [WM-5](#wm-5) | 수유실 데이터 확장 — 역사 밖 시설 조사 | 데이터 | 5d(조사) + 1d(반영) | ⛔ 미착수 — 현장 조사 필요 |
| [WM-6](#wm-6) | 시설 타입 라벨·설명을 부모 관점으로 재작성 | 기능 | 2d | ✅ 구현됨 |
| [WM-7](#wm-7) | 식당 방문 정보 — 스키마·노출·공백 표기 | 기능 | 1d | ✅ 구현됨(데이터는 공백) |
| [WM-8](#wm-8) | 경로 요청 시한 · 계단 고지 | 결함 | 0.5d | ✅ 구현됨 |

> **검증 범위.** 타입체크 · 린트 · 프로덕션 빌드 통과, 서버 렌더 결과로 목록
> 67건(중복 이름 0 · 개찰구 태그 19/19 · 제원 태그 0)까지 확인했습니다.
> **지도·위치·클러스터는 브라우저에서 눈으로 확인하지 않았습니다** — 클러스터
> 펼침, 위치 권한 흐름, 모바일 배너 겹침은 실기기에서 한 번 봐야 합니다.

**남은 것은 WM-5 하나뿐이고, 이것만 코드로 닫을 수 없습니다.** 수유실 20곳은
현장에 가서 세어야 하는 값이라, 지어내면 없는 수유실로 유모차를 밀고 가게
됩니다. 조사만 채우면 반영은 1일입니다.

---

<a id="wm-1"></a>
## WM-1 · 내 위치 상시 표시 · 위치 권한 단일 진입점

**유형** 기능 · **우선순위** P0 · **추정** 3d · **영역** frontend

### 배경

현재 위치 정보는 **길찾기를 누른 순간에만, 목적지 하나당 1회씩** 사용됩니다
(`frontend/src/components/MapView.tsx:327`). 지도를 열었을 때 사용자는 자기가
어디 있는지 지도 위에서 알 수 없고, "가까운 곳"이라는 판단을 눈대중으로 해야
합니다.

이 서비스를 여는 전형적 순간은 *"지금 여기서 제일 가까운 수유실"* 입니다.
위치는 부가 기능이 아니라 **모든 화면의 기준점**이어야 합니다.

### 사용자 스토리

> 유모차를 밀고 위례중앙광장에 있는 부모로서, 지도를 열자마자 내가 어디 있는지
> 보이길 원한다. 그래야 목록에 뜬 시설이 나한테 가까운지 먼지 판단할 수 있다.

### 범위

**In**
- 최초 진입 시 위치 권한 요청 (거부 가능한 비차단 UI — 지도 위 배너 또는 컨트롤)
- 허용 시 내 위치 마커 상시 표시 + 지도 초기 중심을 내 위치로
- "내 위치로 이동" 지도 컨트롤 버튼
- 권한 상태를 앱 레벨 단일 소스로 관리 (`useUserLocation` 훅 신설)
- 길찾기 플로우가 이 훅을 재사용 — 목적지마다 재확인하지 않음
- `watchPosition` 대신 `getCurrentPosition` + 수동 갱신 (배터리)

**Out**
- 실시간 위치 추적/따라가기
- 반경 필터 (별도 티켓, P1)

### 수용 기준

1. 첫 진입 시 위치 권한을 **1회** 요청하고, 거부해도 지도·목록 전 기능이 동작한다.
2. 허용 상태에서 지도 초기 중심이 내 위치이고, 위치 마커가 표시된다.
   단 내 위치가 데이터 커버리지 밖(위례 생활권 반경 5km 초과)이면
   기존 `HOME_CENTER`를 유지하고 안내 문구를 띄운다.
3. 길찾기를 연속 3개 목적지에 대해 실행해도 권한 확인 카드가 다시 뜨지 않는다.
4. 권한 거부 상태에서 길찾기를 누르면 **"권한 거부됨"과 "네트워크 오류"를 구분한**
   메시지가 뜬다. (현재 `MapView.tsx:366`은 둘을 하나로 뭉갬)
5. 위치 정확도가 100m를 넘으면 마커에 정확도 원을 함께 그린다.

### 기술 노트

- `MapView.tsx:257-295`의 `routeToId` 이펙트에서 권한 조회 로직을 분리해 훅으로 승격
- `userLocationIcon()` (`MapView.tsx:44`)은 이미 있으므로 재사용
- `navigator.permissions.query`는 Safari 지원이 불완전 → 실패 시 낙관적 요청으로 폴백 (현행 `catch` 유지)

---

<a id="wm-2"></a>
## WM-2 · 목록 거리순 정렬 (기본 정렬 전환)

**유형** 기능 · **우선순위** P0 · **추정** 2d · **의존성** WM-1

### 배경

목록 순서가 `places` 배열의 원본 순서 그대로입니다
(`frontend/src/app/page.tsx:60`). 이 순서는 조사 자료를 입력한 순서일 뿐
사용자에게 아무 의미가 없습니다. 67곳 중 위쪽 5~6개만 실질적으로 노출되는데,
그 5~6개가 사용자와 무관하게 결정되고 있습니다.

### 사용자 스토리

> 부모로서, 목록 맨 위에 나한테서 제일 가까운 곳이 오길 원한다. 스크롤하며
> 주소를 읽고 거리를 머릿속으로 계산하고 싶지 않다.

### 범위

**In**
- 정렬 기준 `거리순`(기본) / `이름순` 토글
- 각 카드에 직선거리 표시 (`320m`, `1.2km`)
- 위치 권한 없음/거부 시 `이름순`으로 폴백하고 토글의 `거리순`은 비활성 + 사유 표시
- 좌표 없는 항목(현재 식당 5곳)은 거리순에서 **맨 뒤 그룹**으로 밀되 숨기지 않음

**Out**
- 도보 실거리/소요시간 기준 정렬 (경로 API를 67회 호출해야 함 — 비현실적)
- 반경 필터

### 수용 기준

1. 위치 허용 상태에서 목록 첫 항목이 실제 최단거리 시설이다.
2. 각 카드에 직선거리가 표시되고, **"직선거리"임이 명시**된다.
   (도보 거리로 오해하면 "300m라며 10분 걸었다"가 됨)
3. 좌표 없는 항목은 거리 자리에 `위치 미확인`을 표시하고 목록 하단에 모인다.
4. 필터 칩의 개수 배지(`page.tsx:48`)는 정렬과 무관하게 기존 동작을 유지한다.
5. 정렬 기준 변경 시 지도 `fitBounds`는 다시 실행되지 않는다.
   (`MapView.tsx:220`의 `fittedKeyRef` 키는 id 집합 기준이므로 순서 변경에 반응하지 않아야 함 — 회귀 확인)

### 기술 노트

- 거리 계산은 Haversine으로 충분. 위례 생활권 스케일에서 오차 무시 가능
- `visiblePlaces` useMemo(`page.tsx:60`)에 정렬 단계 추가
- 정렬 상태를 URL 쿼리에 넣지 않음 (딥링크는 별도 P2 티켓)

---

<a id="wm-3"></a>
## WM-3 · 마커 클러스터링 — 겹친 21곳 선택 가능하게

**유형** 결함 · **우선순위** P0 · **추정** 3d

### 배경 — 재현된 사실

식당 좌표는 주소 지오코딩으로 얻은 **건물 단위** 좌표라(`places.ts:29-34`),
한 건물의 여러 식당이 완전히 동일한 좌표를 갖습니다.

```
9곳 → 37.477863, 127.126004
7곳 → 37.480472, 127.124143
3곳 → 37.474597, 127.143235
2곳 → 37.477649, 127.124994
```

**총 21곳이 4개 지점에 완전히 포개져 있습니다.** 클러스터링이나 스파이더파이가
없어 맨 위 마커 하나만 클릭되고, 나머지 17곳은 지도상에서 선택할 방법이
없습니다. 목록 카드를 눌러도 `setView` 후 해당 마커 팝업만 열리므로
(`MapView.tsx:242-250`) 지도에서 옆 가게로 넘어갈 수 없습니다.

즉 **지도 기능이 데이터의 1/3에 대해 동작하지 않습니다.**

### 범위

**In**
- `leaflet.markercluster` 도입 (또는 동등 기능 직접 구현)
- 동일/근접 좌표 마커는 클러스터 배지로 묶고, 클릭 시 스파이더파이로 펼침
- 클러스터 배지가 디자인 시스템을 따름 — 타입 색 혼합이 아니라 중립 배지 + 개수
  (`frontend/DESIGN.md` §1 "시설의 색은 끝까지 따라간다" 원칙과의 충돌을
  디자인 리뷰에서 확정할 것)
- 목록에서 카드 선택 시, 그 마커가 클러스터에 묶여 있으면 자동으로 스파이더파이 후 팝업 오픈
- 필터 변경 시 클러스터 재계산 (`MapView.tsx:203`의 표시/숨김 로직 대체)

**Out**
- 건물 내 층·호수 단위 좌표 정밀화 (조사 필요, 별도 티켓)

### 수용 기준

1. 가든파이브 9곳이 지도에서 **모두 개별 선택 가능**하다.
2. 클러스터를 펼친 상태에서 필터를 바꾸면 잔상 마커가 남지 않는다.
3. 목록에서 파크하비오 7곳 중 5번째 카드를 누르면 해당 마커의 팝업이 열린다.
4. 마커 40px + 스파이더파이 상태에서 모바일(≤850px, 지도 높이 420px)에서도
   펼쳐진 마커가 뷰포트 안에 들어온다.
5. `fitBounds`(`MapView.tsx:230`)가 클러스터 도입 후에도 동일하게 동작한다.

### 기술 노트

- `markersRef`(`MapView.tsx:133`)가 `Map<string, L.Marker>`로 되어 있어
  클러스터 그룹으로 감싸는 변경 범위는 크지 않음
- StrictMode 이중 마운트 정리 로직(`MapView.tsx:190-199`)에 클러스터 그룹 해제 추가 필요
- **대안 검토**: 마커를 미세하게 흩뿌리는 방식(jitter)은 좌표를 거짓말하게
  만드므로 채택하지 않음. 데이터 정직성 원칙(`DESIGN.md` §6) 위배.

---

<a id="wm-4"></a>
## WM-4 · 동일 시설명 카드 구분

**유형** 결함 · **우선순위** P0 · **추정** 0.5d

### 배경 — 재현된 사실

목록에 **이름이 완전히 동일한 카드가 2장씩** 뜨는 시설이 3쌍 있습니다.

```
2x  거여역 엘리베이터 · 내부
2x  마천역 엘리베이터 · 내부
2x  장지역 엘리베이터 · 내부
```

구분 정보는 `location` 필드(`개롱 방면 6-2` 등)에 들어 있지만, 카드에서는
"위치" 라벨 뒤 보조 텍스트로 렌더링돼(`page.tsx:177-181`) 제목의 중복이
먼저 눈에 들어옵니다. 사용자에게는 **중복 등록된 데이터로 보입니다.**

가장 싸게 고칠 수 있는 P0이므로 첫 번째로 착수합니다.

### 범위

**In**
- 같은 역·같은 타입 내에서 이름이 중복될 때 승강장/방면 정보를 제목에 병합
  → `거여역 엘리베이터 · 개롱 방면 6-2`
- 데이터 레이어에서 처리 (`places.ts`의 `name` 정정) — 렌더링 시점 분기 아님
- 지도 마커 팝업 제목도 동일하게 반영 (`MapView.tsx:92`가 `p.name`을 그대로 쓰므로 자동)

**Out**
- 엘리베이터 데이터 자체의 재구조화 (WM-6에서 다룸)

### 수용 기준

1. 전체 목록에서 이름이 중복되는 카드가 0건이다.
2. 검색어 `거여`로 필터했을 때 두 엘리베이터가 구분되어 읽힌다.
3. 팝업 제목이 288px 폭(`MapView.tsx:178`)에서 2줄을 넘지 않는다.

### 검증 스크립트

```
node -e "const s=require('fs').readFileSync('frontend/src/data/places.ts','utf8');
const n={};[...s.matchAll(/name: \"([^\"]*)\"/g)].forEach(m=>n[m[1]]=(n[m[1]]||0)+1);
console.log(Object.entries(n).filter(([,v])=>v>1))"
```
→ `[]` 이면 통과

---

<a id="wm-5"></a>
## WM-5 · 수유실 데이터 확장 — 역사 밖 시설 조사

**유형** 데이터/조사 · **우선순위** P0 · **추정** 조사 5d + 반영 1d

### 배경 — 재현된 사실

| 타입 | 개수 |
| --- | --- |
| 식당 | 48 |
| 엘리베이터 | 12 |
| 경사로 | 5 |
| **수유실** | **2** |

서비스명이 "아이편한 지도"인데 수유실이 거여·장지 **2곳**뿐입니다. 현재 구성은
조사 가능했던 공공데이터(지하철 시설)와 자체 조사(식당)의 합집합이지,
사용자 필요의 우선순위가 아닙니다. 부모가 수유실을 찾는 실제 장소는
지하철역보다 **대형마트·백화점·도서관·주민센터·공원**입니다.

카테고리를 4개 노출하면서 그중 하나가 2건이면, 그 필터를 누른 사용자는
"이 서비스는 데이터가 없다"고 판단하고 이탈합니다.

### 목표

수유실 **20곳 이상** 확보. 위례 생활권(성남 수정구 창곡동 · 서울 송파구 장지동 ·
하남 학암동) 기준.

### 조사 대상 후보

- 위례중앙광장 일대 상가 · 위례스타필드시티
- 가든파이브 · 파크하비오 (이미 식당 데이터가 있는 건물 — 동선 재사용)
- 창곡동/장지동 주민센터, 위례도서관, 보건소
- 위례중앙광장 공원, 휴먼링 일대
- 대형 병원·소아과 인근

### 항목별 필수 수집 필드

- 정확한 위치 (건물명 + 층 + "화장실 옆" 수준의 랜드마크)
- 운영시간 (상시/영업시간 내/예약제)
- **기저귀 교환대 유무** — 현재 `details` 배열에 있으나 필수 필드로 승격
- 남성 보호자 이용 가능 여부
- 이용 조건 (직원 문의 필요 여부)
- 조사 일자 · 조사자

### 수용 기준

1. 수유실 20곳 이상, 전부 좌표 보유 (건물 단위가 아니라 출입구 기준).
2. 각 항목에 조사 일자가 기록된다 — `Place`에 `surveyedAt` 필드 신설.
3. 수유실 필터를 눌렀을 때 위례중앙광장 반경 1km 안에 최소 5곳이 표시된다.

### 리스크

- **리드타임이 가장 김.** 개발 티켓과 병렬로 오늘 착수해야 v0.2에 들어감.
- 상업시설 수유실은 운영 정책이 자주 바뀜 → `surveyedAt` 노출과 제보 창구(P2)가
  뒤따르지 않으면 6개월 내 신뢰도 하락.

---

<a id="wm-6"></a>
## WM-6 · 시설 타입 라벨·설명을 부모 관점으로 재작성

**유형** 기능 · **우선순위** P0 · **추정** 2d

### 배경

경사로·엘리베이터 데이터가 **원본 공공데이터의 휠체어 관점 그대로** 노출됩니다.

```
name:    "거여역 휠체어 경사로"
details: ["정원 15명", "정격하중 1,000kg"]
note:    "유모차 이용 시 혼잡 시간대를 고려해 주세요."
```

유모차를 미는 부모에게 **정격하중 1,000kg은 아무 의미가 없습니다.** 부모가 실제로
묻는 것은 다음입니다.

- 몇 번 출구에 엘리베이터가 있나
- 개찰구 **안**이야 **밖**이야 (표를 찍기 전인가 후인가)
- 계단만 있는 출구는 어디인가
- 유모차를 접지 않고 탈 수 있나

동시에 — 휠체어 데이터를 실었다면 휠체어 이용자도 잠재 사용자인데, UI 문구는
전부 "아이와 함께"입니다(`page.tsx:92`). **대상이 둘인데 어느 쪽에도 맞지 않는
상태**이므로, 이 티켓에서 대상을 명시적으로 확정합니다.

### 선행 결정 (기획 판단 필요 — 개발 착수 전 확정할 것)

> **Q. 이 서비스의 1차 대상은 영유아 동반 보호자 단일인가, 이동약자 전체인가?**
>
> - **(a) 영유아 단독** — 휠체어 원문 필드를 유모차 관점으로 번역해 노출.
>   범위가 좁고 메시지가 선명함. 캠페인 성격에 부합.
> - **(b) 이동약자 전체** — 헤더 카피·아이콘·태그라인까지 전면 개편.
>   데이터 활용도는 높지만 v0.2 범위를 크게 넘김.
>
> **권고: (a).** 캠페인 기간과 조사 자료의 성격상 (a)로 좁히고, (b)는 로드맵에 남긴다.

### 범위 — (a) 기준

**In**
- `details`에서 부모와 무관한 제원(정격하중, 정원) 제거 또는 접기
- 엘리베이터 항목에 **개찰구 안/밖**, **연결 출구 번호** 필드 추가
  (원본 데이터에 없으면 WM-5 조사에 포함)
- `note` 문구를 "현장 위치를 확인해 주세요" 같은 면피성 문장에서
  실제 판단에 쓸 수 있는 정보로 교체
- "휠체어 경사로" → 유모차·휠체어 공용임을 밝히되 제목은 사용 맥락 중심으로

**Out**
- 아이콘 재제작 (`DESIGN.md` §4 — 원본 아트 재작업 금지)
- 헤더/브랜드 카피 전면 개편 (선행 결정이 (b)일 때만)

### 수용 기준

1. 엘리베이터 카드에서 `정격하중`·`정원`이 1차 노출에서 사라진다.
2. 엘리베이터 12건 전부에 개찰구 안/밖 정보가 있거나, 없으면 `확인 필요` 태그가 붙는다.
   (`chipsFor` 한 곳에서 판정 — `places.ts:57`의 기존 패턴 유지)
3. 서비스 대상 정의가 `DESIGN.md`에 한 문단으로 기록된다.

### 기술 노트

- 판정 로직은 `chipsFor`(`places.ts:57`)에 모으는 기존 규칙을 지킬 것.
  `DESIGN.md` §6의 "판정은 한 곳에서만" 원칙.
- 타입 추가가 아니므로 `DESIGN.md` §8 절차는 해당 없음.

---

---

<a id="wm-7"></a>
## WM-7 · 식당 방문 정보 — 스키마·노출·공백 표기

**유형** 기능 · **우선순위** P1 · **추정** 1d · **상태** 구현됨(데이터는 공백)

### 배경 — 재현된 사실

`extra` 필드 48건을 훑은 결과입니다.

```
영업시간 언급   1건 / 48
전화번호 언급   0건 / 48
기저귀 교환대   0건 / 48   (수유실 2곳의 details 에만 존재)
```

`KidFriendly` 는 유아의자 · 유아식기 · 유모차 세 항목뿐입니다. **헛걸음의 가장
큰 원인인 영업시간과, 조건부 판정("유모차 확인 필요")을 실제로 풀어 주는 유일한
수단인 전화번호가 둘 다 없습니다.** 아이를 데리고 나선 뒤의 헛걸음은 어른
혼자일 때와 무게가 다릅니다.

### 이 티켓이 한 것 / 하지 않은 것

데이터를 만들 수는 없으므로, **빈칸이 "조사했더니 없더라"로 읽히는 것을
막는 데까지**만 했습니다.

- `Place` 에 `hours` · `phone`, `KidFriendly` 에 `changingTable` 추가
- 값이 들어오면 카드·팝업이 알아서 보여 주도록 렌더링을 미리 붙임 — 조사만
  채우면 코드를 고칠 일이 없다
- `changingTable` 은 `false`(없음)와 `undefined`(미조사)를 구분. 나머지 세
  항목은 조사 원문이 boolean 이라 `false` 가 곧 '없음'이고, 둘을 같은 칩 줄에
  섞으면 무엇이 확인된 사실인지 알 수 없게 된다
- `unsurveyedFor()` — 무엇이 조사되지 않았는지 이름 붙여 팝업에 한 줄로
- 칩 순서 재정렬: **유모차 → 기저귀 → 유아의자 → 유아식기.** 카드에는 세 개만
  보이므로(`MAX_CARD_CHIPS`) 문 앞에서 되돌아가게 만드는 것부터 앞에 둔다

### 왜 "기저귀 확인 필요" 칩을 48곳 전부에 달지 않았나

붙여 봤다면 48장 전부에 같은 태그가 뜹니다. 매 카드에 반복되는 문구는 정보가
아니라 배경 소음이 되고, 정작 읽어야 할 **유모차 반입** 태그를 세 칸 밖으로
밀어냅니다. 모른다는 사실은 팝업에서 한 번만 말합니다.

### 남은 일 (WM-5 조사에 합칠 것)

48곳의 영업시간 · 브레이크타임 · 전화번호 · 기저귀 교환대. 네이버 지도 링크가
이미 48건 전부에 있으므로 조사 자체는 대부분 책상에서 가능합니다.

---

<a id="wm-8"></a>
## WM-8 · 경로 요청 시한 · 계단 고지

**유형** 결함 · **우선순위** P1 · **추정** 0.5d · **상태** 구현됨

### 배경

두 가지가 겹쳐 있었습니다.

**1. 시한이 없었다.** `routing.openstreetmap.de` 는 무료 공개 인스턴스라 SLA 가
없고, 느려질 때는 응답이 오지 않는 게 아니라 아주 늦게 옵니다. `fetch` 에
시한이 없어 **"경로 찾는 중…" 이 영원히 돌 수 있었습니다.** 사용자는 앱이 멈춘
줄 압니다.

**2. 유모차 경로가 아니다.** `routed-foot` 은 일반 보행자 프로파일이라
계단·육교를 피하지 않습니다. 한국 OSM 의 계단 태깅은 성글어서 **유모차로 못 가는
길을 "도보 8분"이라고 안내할 수 있습니다.** 이건 오차가 아니라 안전 문제입니다.

### 한 것

- `AbortController` + 12초 시한. 시한 초과와 연결 실패를 다른 문구로 나눔
  (`timeout` / `network`) — 전자는 기다렸다 다시 누르면 되고 후자는 내 인터넷을
  봐야 하므로, 사용자가 할 일이 다르다
- **계단 고지를 경로 결과 카드로 올림.** 페이지 맨 아래 이용 안내는 이미 걷기
  시작한 사람에게 닿지 않는다
- 고지에 경고색(빨강)을 쓰지 않음 — 길찾기가 실패한 게 아니고, 매번 뜨는 문구를
  경고로 칠하면 두 번째부터는 아무도 읽지 않는다

### 하지 못한 것

**계단이 실제로 있는지 알아낼 방법이 없습니다.** OSRM 응답에는 `highway=steps`
여부가 실려 오지 않아, "이 경로에 계단 있음"을 판정할 수 없습니다. 그래서 조건
없이 항상 고지합니다. 정확히 아는 방법은 유모차 프로파일을 직접 굴리거나
(자체 OSRM 인스턴스 + 커스텀 profile.lua) 상용 API 로 옮기는 것이고, 둘 다
v0.2 범위 밖입니다.

---

## 범위 밖이지만 P0와 얽힌 것

착수 전 알고 있어야 할 인접 이슈. 별도 티켓으로 끊되 P0 작업 중 마주칠 수 있음.

| 이슈 | 왜 P0와 얽히나 |
| --- | --- |
| `DESIGN.md` §6 수치가 실제 데이터와 불일치 (문서 `68개·지도 27곳` vs 실제 `67개·62곳`) | WM-2/WM-3에서 이 문서를 근거로 삼게 됨. 착수 시 함께 정정 |
| 길찾기가 유모차 경로가 아님 (`routed-foot` 일반 보행자 프로파일) | WM-1이 위치 플로우를 건드리므로 같은 코드 경로. 최소한 "계단 포함 가능" 고지 추가 검토 |
| `surveyedAt` 부재 | WM-5의 수용 기준에 포함시켰으므로 `Place` 타입 변경이 선행 |
| CORS `localhost:3000` 하드코딩 (`backend/.../WebConfig.java:13`) | v0.2 배포 시 즉시 깨짐. 배포 티켓에서 처리 |
